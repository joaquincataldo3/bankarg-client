/* import { configureStore } from "@reduxjs/toolkit";

const initialState = {
    isMobileNavbarOpen: false,
    isDesktopNavbarOpen: false
}

const headerSlice = configureStore({
    header: 'header',
    initialState
})

export default headerSlice */